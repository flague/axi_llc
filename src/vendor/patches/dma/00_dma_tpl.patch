diff --git a/data/dma.hjson.tpl b/data/dma.hjson.tpl
index f8eb1a0..0253d7a 100644
--- a/data/dma.hjson.tpl
+++ b/data/dma.hjson.tpl
@@ -2,13 +2,6 @@
 // Licensed under the Apache License, Version 2.0, see LICENSE for details.
 // SPDX-License-Identifier: Apache-2.0
 
-<%
-  dma = xheep.get_base_peripheral_domain().get_dma()
-  dma_addr_mode = dma.get_addr_mode() == 1
-  dma_subaddr_mode = dma.get_subaddr_mode() == 1
-  dma_hw_fifo_mode = dma.get_hw_fifo_mode() == 1
-  dma_zero_padding = dma.get_zero_padding() == 1
-%>
 
 { name: "dma"
   clock_primary: "clk_i"
diff --git a/data/dma_conf.svh.tpl b/data/dma_conf.svh.tpl
index 362b5c4..45dec52 100644
--- a/data/dma_conf.svh.tpl
+++ b/data/dma_conf.svh.tpl
@@ -9,13 +9,6 @@
  * Info: Header file for the DMA subsystem.
  */
 
-<% 
-    dma = xheep.get_base_peripheral_domain().get_dma()
-    dma_addr_mode = dma.get_addr_mode()
-    dma_zero_padding = dma.get_zero_padding()
-    dma_subaddr_mode = dma.get_subaddr_mode()
-    dma_hw_fifo_mode = dma.get_hw_fifo_mode()
-%>
 
 % if dma_addr_mode:
 `define ADDR_MODE_EN
